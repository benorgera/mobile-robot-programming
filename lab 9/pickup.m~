classdef pickup < handle
    properties(Access = public)
         robot
    end
    methods(Access = public)
        function obj = pickup()
            global robot;
            obj.robot = robot;
        end
        
        function moveRelDist(obj, dist, doPlot)
            sgn = 
            ref = trapezoidalStepReferenceControl(0.25, 0.5, abs(dist), ...
                1, 0);
        end
        
    end
end